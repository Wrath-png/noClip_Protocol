using UnityEngine;

//Will be attached to "play Game" button in the main menu
//Will need "Startlevel" Script on all entities that saves their og position before continuing:
//Every level will begin with walls, enemies, and other models being unactivated
//and given a random Y value and speed at which they will all move into place
//Floor and Ceiling(lights on ceiling) will be assigned random z and X values and slid into place

public class LevelStartSequence : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
